import React from 'react';
import '../../styles/Main.css';

const Main = () => {
  return (
    <div className={'Main'}>
      <input type={'text'} placeholder={'some searching request'} className={'search-bar'} />
    </div>
  );
};

export default Main;
